#include<stdio.h>
#include<string.h>

int main()
{
unsigned int i;
char nombre[15]; 
printf("Escriba un nombre:");
scanf("%s",nombre);
printf("Largo de la cadena:%d\n",strlen(nombre));
for ( i = 0; i < strlen(nombre); i++)
{
 printf("%c.",nombre[i]);
}
return 0;
}


